{
  "scripts": {
    "buildCustom": "cd client && react-scripts build",
    "client": "cd client && npm start",
    "server": "cd server && nodemon app.js",
    "dev": "concurrently \"npm run server\" \"npm run client\""
    },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
  
}
